name: Table of Contents PR

on:
  push:
    paths:
    - 'README.md'
    - 'README.md'
  pull_request:
    paths:
    - 'README.md'
    - 'README.md'

jobs:
  generateTOC:
    name: TOC Generator
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v2
